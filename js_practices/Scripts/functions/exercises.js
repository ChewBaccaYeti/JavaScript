// =============================================================================
// FUNCTIONS MASTERY — exercises.js
// =============================================================================
// Правила:
//   • Каждая задача — отдельная функция с именем taskN()
//   • Вызывай функцию внизу файла для теста: taskN()
//   • Условие содержит «Инструменты» — ТОЛЬКО их используй в этой задаче
//   • «Hints» — подсказка к синтаксису, не к решению
// =============================================================================

// ─────────────────────────────────────────────────────────────────────────────
// БЛОК 1 — Function Declaration vs Expression
// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 1 — Function Declaration (базовая)
 * Напиши функцию которая возвращает сумму двух чисел.
 * Вызови её и выведи результат.
 * Инструменты: function declaration, return
 * Hints: function add(a, b) { return a + b; }
 */
function task1() {
    function add(a, b) {
        return a + b;
    }
    console.log(add(6, 3));
}

// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 2 — Function Expression
 * Напиши функцию-выражение (переменной) которая проверяет чётность числа.
 * Возвращай true если чётное, false если нечётное.
 * Инструменты: const, function expression, return
 * Hints: const isEven = function(n) { return n % 2 === 0; }
 */
function task2() {
    function isEven(n) {
        if (n % 2 === 0) {
            return true;
        }
        return false;
    }
    console.log(isEven(4));
    console.log(isEven(7));
}

// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 3 — Hoisting: Declaration vs Expression
 * Вызови функцию ДО того как её объявить. Узнаешь когда работает, когда нет.
 * Функция должна возвращать строку "Привет!"
 * Инструменты: function declaration, function expression, console.log
 * Hints: declaration поднимается (hoisting), expression нет
 */
function task3() {
    sayHi(); // вызови sayHi() здесь (перед объявлением)
    function sayHi() {
        // объяви function declaration sayHi()
        return console.log('Привет из функции!');
    }
    // sayHello(); // попробуй вызвать sayHello() здесь (используя expression), спойлер - не сработает
    const sayHello = function () {
        // const sayHello = function() { return "Привет!"; }
        return console.log('Привет из функционального выражения!');
    };
    sayHello();
}

// ─────────────────────────────────────────────────────────────────────────────
// БЛОК 2 — Arrow Functions
// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 4 — Arrow Function (однострочная)
 * Напиши стрелочную функцию которая возвращает куб числа (n³).
 * Вызови для чисел 2, 3, 5.
 * Инструменты: const, arrow function (=>), return (неявный)
 * Hints: const cube = n => n * n * n;
 */
function task4() {
    const cube = n => {
        return n * n * n;
    };
    console.log(cube(2));
    console.log(cube(3));
    console.log(cube(5));
}

// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 5 — Arrow Function (многострочная с блоком)
 * Напиши стрелочную функцию которая принимает массив и выводит каждый элемент.
 * Инструменты: const, arrow function, for...of, блок { }
 * Hints: const printArray = (arr) => { for (const item of arr) { ... } }
 */
function task5() {
    const printArray = array => {
        for (const item of array) {
            console.log(item);
        }
    };
    printArray([1, 2, 3]);
}

// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 6 — Arrow Function и `this` (лексическое связывание)
 * Объект с методом использует стрелочную функцию внутри.
 * Покажи что `this` указывает на объект (не на undefined).
 * Инструменты: const, arrow function, this, объект
 * Hints: стрелочная функция наследует `this` от контекста
 */
function task6() {
    // создай объект с методом, который использует стрелочную функцию
    // const obj = { name: "Test", greet: () => { console.log(this); } }
    // вызови obj.greet()
    const obj = {
        name: 'Test',
        greet: () => {
            console.log(this);
        },
    };
    obj.greet();
}

// ─────────────────────────────────────────────────────────────────────────────
// БЛОК 3 — Parameters (параметры)
// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 7 — Default Parameters
 * Напиши функцию которая приветствует пользователя.
 * Если имя не передано, используй "Гость" по умолчанию.
 * Инструменты: function, default parameter
 * Hints: function greet(name = "Гость") { return `Привет, ${name}!`; }
 */
function task7() {
    // напиши функцию greet с параметром по умолчанию
    // вызови greet() и greet("Иван"), выведи
}

// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 8 — Rest Parameters (остаток параметров)
 * Напиши функцию которая складывает любое количество чисел.
 * Инструменты: function, ...args, for...of
 * Hints: function sum(...numbers) { let total = 0; for (const n of numbers) ... }
 */
function task8() {
    // напиши функцию sum с rest параметром
    // вызови sum(1, 2, 3) и sum(5, 10, 15, 20), выведи суммы
}

// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 9 — Spread Operator (разворот)
 * Дан массив чисел. Найди максимум используя spread и Math.max.
 * Инструменты: const, Math.max, spread operator ...
 * Hints: const arr = [1, 5, 3, 9, 2]; Math.max(...arr);
 */
function task9() {
    // напиши код который использует spread в Math.max
    // const arr = [1, 5, 3, 9, 2];
    // выведи максимум
}

// ─────────────────────────────────────────────────────────────────────────────
// БЛОК 4 — Return и Side Effects
// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 10 — Early Return (ранний выход)
 * Напиши функцию которая проверяет пароль.
 * Если пустой → верни ошибку сразу.
 * Если < 8 символов → верни ошибку.
 * Иначе → верни "OK".
 * Инструменты: if, return, function
 * Hints: если условие true → return; не нужен else для остального кода
 */
function task10() {
    // напиши функцию validatePassword
    // вызови для "", "123", "mypass123", выведи результаты
}

// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 11 — Pure Function (чистая функция)
 * Напиши функцию которая умножает число на 2 (не изменяет исходные данные).
 * Вызови несколько раз для одного числа — результат должен быть одинаковый.
 * Инструменты: function, return
 * Hints: чистая функция: одинаковый input → одинаковый output, нет side effects
 */
function task11() {
    // напиши pure функцию double
    // вызови несколько раз для одного числа и убедись результаты одинаковые
}

// ─────────────────────────────────────────────────────────────────────────────
// БЛОК 5 — Closures и Scope
// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 12 — Closure: Counter (счётчик)
 * Напиши функцию которая возвращает функцию-счётчик.
 * Каждый вызов счётчика увеличивает счёт на 1.
 * Инструменты: function, return function, closure, let
 * Hints: function makeCounter() { let count = 0; return () => ++count; }
 */
function task12() {
    // напиши функцию makeCounter
    // const counter = makeCounter();
    // вызови counter() несколько раз и выведи результаты
}

// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 13 — Closure: Private State
 * Напиши функцию которая создаёт объект с приватным балансом.
 * Методы: deposit (положить), withdraw (снять), getBalance (вывести баланс).
 * Инструменты: function, return object, closure
 * Hints: function createAccount(initial) { let balance = initial; return { ... } }
 */
function task13() {
    // напиши функцию createAccount
    // const acc = createAccount(100);
    // вызови acc.deposit(50), acc.withdraw(30), acc.getBalance()
}

// ─────────────────────────────────────────────────────────────────────────────
// БЛОК 6 — IIFE и Functional Patterns
// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 14 — IIFE (Immediately Invoked Function Expression)
 * Напиши IIFE которая выводит "Я выполнилась сразу!".
 * Инструменты: function expression, IIFE pattern ()()
 * Hints: (function() { ... })();
 */
function task14() {
    // напиши IIFE которая выводит сообщение
}

// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 15 — Callback Function (обратный вызов)
 * Напиши функцию которая принимает число и callback.
 * Если число > 0 → вызови callback с сообщением "Положительное!".
 * Инструменты: function, callback параметр, if
 * Hints: function check(n, callback) { if (n > 0) callback("Положительное!"); }
 */
function task15() {
    // напиши функцию check с callback параметром
    // вызови check(5, (msg) => console.log(msg))
}

// ─────────────────────────────────────────────────────────────────────────────

/**
 * ЗАДАЧА 16 — Higher-Order Function (функция высшего порядка)
 * Напиши функцию которая возвращает функцию-умножитель.
 * multiply(3) должна возвращать функцию которая умножает на 3.
 * Инструменты: function, return function, closure
 * Hints: function multiply(n) { return (x) => x * n; }
 */
function task16() {
    // напиши функцию multiply
    // const double = multiply(2);
    // вызови double(5), double(10), выведи результаты
}

// =============================================================================
// ЗАПУСК — раскомментируй нужную задачу
// =============================================================================

task1();
task2();
task3();
task4();
task5();
task6();
task7();
task8();
task9();
task10();
task11();
task12();
task13();
task14();
task15();
task16();
